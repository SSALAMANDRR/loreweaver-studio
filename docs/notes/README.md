# Agent Notes

Decision records for non-trivial changes in this repo: `implemented/` holds one
note per landed mechanism, written in the present tense.

House format, same as the engine repo's `docs/notes/implemented/`: five lines —
problem / decision / reason / rule home / date. A note records the WHY and WHEN;
the rule text itself lives in exactly one home, the module that owns it.
